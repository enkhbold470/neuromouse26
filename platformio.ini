; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

; ── Shared settings ───────────────────────────────────────────────────────────
[platformio]
default_envs = test-motor-ble-drive

[common]
platform = espressif32
board    = esp32-s3-devkitc-1
framework = arduino
board_build.f_cpu = 160000000L
extra_scripts = post:notify_upload.py
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
monitor_speed = 115200
flash_speed = 80000000L
lib_deps =
    fastled/FastLED @ ^3.6.0
    h2zero/NimBLE-Arduino @ ^1.4.2

; ── Main firmware ─────────────────────────────────────────────────────────────
; [env:main]
; extends = common
; build_src_filter = +<main.cpp> +<MicromouseEncoder.cpp>

; ── Hardware tests (flash with: pio run -e <name> -t upload) ──────────────────

; [env:test-motor-enc]
; extends = common
; build_src_filter = -<*> +<../test/motor-and-enc.cpp> +<MicromouseEncoder.cpp>


; [env:test-imu]
; extends = common
; build_src_filter = -<*> +<../test/mpu6500.cpp>

; [env:test-ble]
; extends = common
; build_src_filter = -<*> +<../test/ble-test.cpp>

; [env:test-batt]
; extends = common
; build_src_filter = -<*> +<../test/batt-volt.cpp>

; [env:test-buzzer]
; extends = common
; build_src_filter = -<*> +<../test/buzzer.cpp>

; [env:test-leds]
; extends = common
; build_src_filter = -<*> +<../test/ws2812b.cpp>

; [env:main]
; extends = common
; build_src_filter = +<main.cpp> +<MicromouseEncoder.cpp>

[env:test-motor-ble-drive]
extends = common
build_src_filter = -<*> +<../test/motor-ble-drive.cpp> +<MicromouseEncoder.cpp>

; [env:test-ir]
; extends = common
; build_src_filter = -<*> +<../test/ir-test.cpp>
